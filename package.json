{
  "name": "invoca-toolkit",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "postinstall": "sh ./scripts/link-env.sh",
    "build": "bun run --filter '*' build",
    "dev:mcp": "bun run --filter '@invoca-toolkit/mcp' dev",
    "dev:cli": "bun run --filter '@invoca-toolkit/cli' dev",
    "test": "bun run --filter '*' test",
    "test:e2e": "bun run --filter '@invoca-toolkit/sdk' test:e2e",
    "lint": "bun run --filter '*' lint",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest"
  }
}
